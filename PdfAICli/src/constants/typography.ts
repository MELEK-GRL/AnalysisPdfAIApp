/**
 * App genelinde tek yerden kontrol edilen tipografi standartları.
 * Tüm text boyutları bu constants'tan beslenir.
 * Kaynak: theme/typography.ts (ekran ölçeklemesi useTypography() ile).
 */
export { baseFontSize as fontSize } from '../theme/typography';
export type { FontSizeKey } from '../theme/typography';
