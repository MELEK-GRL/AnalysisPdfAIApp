/** Default API request timeout in ms (Railway soğuk başlangıç için 30s) */
export const API_TIMEOUT_MS = 30_000;

/** Upload request timeout in ms (PDF analysis + OpenAI can take 2+ min) */
export const UPLOAD_TIMEOUT_MS = 180_000;
